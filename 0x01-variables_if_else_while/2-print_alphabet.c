#include <stdio.h>

/**
 * main - Program prints each letter of alphabet
 * Description: Program uses a for loop
 * Return: 0(Success)
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++;)
	{
		putchar('i');
		putchar('\n');
	}
	return (0);
}
